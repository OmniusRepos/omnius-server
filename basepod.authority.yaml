name: omnius-license
server: bp.common.al
port: 8080
domains:
  - license.omnius.lol
build:
    dockerfile: Dockerfile
    context: .
env:
  LICENSE_SERVER_MODE: "true"
volumes:
  - license-data:/app/data
